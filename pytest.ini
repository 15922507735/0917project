[pytest]
# 测试用例搜索路径：仅在 testcase_manage 下收集 test_*.py
testpaths = testcase_manage
# 默认命令行参数：详细模式 + 短堆栈
addopts = -v --tb=short
# 自定义标记：避免 pytest 报 "unknown marker" 警告
markers =
    smoke: 冒烟用例（核心路径，快速验证）
    regression: 回归用例（覆盖度更广）
    business: 业务用例（与具体业务模块相关）
